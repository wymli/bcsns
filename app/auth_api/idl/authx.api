syntax = "v1"

info(
	title: "auth"
	desc: "auth"
	author: "liwm29"
	email: "liwm29@mail2.sysu.edu.cn"
	version: "v1"
)

type (
	ValidateTokenReq {
	}
	ValidateTokenResp {
		UserId uint64 `json:"user_id"`
		Ok     bool  `json:"ok"`
	}
)

@server(
	group: validate
	prefix: api/v1
)
service auth-api {
	
	@doc "validate"
	@handler validate
	get /auth/validate (ValidateTokenReq) returns (ValidateTokenResp)
}